class Solution {
public:
    bool search(vector<int>& nums, int target) {
        int size = nums.size();
        for (int i = 0; i < size; i++) {
            if (target == nums[i]) {
                return true;
            }
        }
        return false;
        
    }
};
